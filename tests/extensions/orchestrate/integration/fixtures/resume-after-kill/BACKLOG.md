# Fixture: resume after a mid-run kill

Two features. The first run is killed after feature 001's BA returns and
feature 001's Dev is mid-flight. A second run with the same BACKLOG.md should
pick up: feature 001 completes, feature 002 starts fresh.

- [ ] Add user login — Support email+password.
- [ ] Profile page — Display user data.
